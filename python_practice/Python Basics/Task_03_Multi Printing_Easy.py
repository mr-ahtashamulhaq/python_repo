# Given two inputs that are stored in variables a and n, you need to print a, n times in a single line without space between them.
#  Output must have a newline at the end.

a=input("Enter a value : ")
n=int(input("Enter n value : "))

print(a*n)